
.. docs on mincfg.configs

MergedConfiguration
###################

Currently the only way to put together :doc:`sources`, so every configuration is a MergedConfiguration.

.. autoclass:: mincfg.MergedConfiguration
   :members:

